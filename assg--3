#include<stdio.h>
#include<math.h>
main()
{
	int n,i;
	printf("Enter the number\n");
	scanf("%d",&n);
    int c=1;
	for(i=2;i<=sqrt(n);i++)
	{
		if(n%i==0)
		{
			c++;
		}
	}
	if(c==1)
	{
		printf("PRIME");
	}
	else
	{
		printf("NOT PRIME");
	}
}
